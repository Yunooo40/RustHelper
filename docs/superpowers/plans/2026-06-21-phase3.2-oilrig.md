# Phase 3.2 — Auto Oil Rig crate hack — Plan

Date : 2026-06-21 · Branche : `feat/phase3.2-oilrig` · Spec :
[specs/2026-06-21-phase3.2-oilrig-design.md](../specs/2026-06-21-phase3.2-oilrig-design.md)

100% plugin C# (aucun changement API) → **pas de surface unit-testable** ; la suite
JS reste à 23/23. Vérification réelle = serveur Rust live (indisponible ici).

## Checklist
- [x] Plugin v0.3.0 : `using UnityEngine;`, bump version, en-tête doc.
- [x] Config `Auto-detect Oil Rig crate hacks` (bool, défaut true).
- [x] Hook `OnCrateHack(HackableLockedCrate)` :
  - filtre Oil Rig via `WhichOilRig(pos)` (`TerrainMeta.Path.Monuments` + `IsInBounds`,
    `oilrig_1`→small / `oilrig_2`→large) ; sinon ignoré.
  - durée via `OilRigUnlockSeconds` (`requiredHackSeconds - hackSeconds`, fallback config/900s).
  - `PostEvent(ev, "called", now, now+remaining, source:null, reporter:null)` (event auto).
  - logs `Debug` (détecté / ignoré).
- [x] Tout wrappé/guardé (drift Rust) : early-return si `TerrainMeta`/monuments null, try/catch sur les champs.
- [x] Docs : `plugin/README.md` (note + config), `README.md` (roadmap), spec + plan.
- [x] `npm test` → 23/23 (inchangé, sanity).

## ⚠️ Vérifs live (bloquantes pour cocher 3.2 [x])
1. Charger le plugin, `Debug=true`.
2. Hacker une crate sur Small puis Large Oil Rig → log « Oil Rig crate hack detected:
   oil_rig_small/large » + notif Discord avec countdown ~15 min.
3. Vérifier qu'une crate hackée hors Oil Rig (heli/cargo) est ignorée.
4. Si les noms de monuments diffèrent, ajuster `WhichOilRig` (matching `Contains`).

## Reporté
- Deep Sea Loot auto-detect (reste `!deep`). Ouverture/looting de crate.
