# Phase 4.1 — Player linking — Plan

Date : 2026-06-21 · Branche : `feat/phase4-linking` (depuis `main`) · Spec :
[specs/2026-06-21-phase4.1-linking-design.md](../specs/2026-06-21-phase4.1-linking-design.md)

TDD côté API/modèle (testable) ; plugin via fixture-contrat.

## Checklist
- [x] `backend/db.js` : tables `link_codes` + `links` (1-to-1).
- [x] Tests rouges `tests/backend/link.test.js` + fixture `link-payloads.js` ;
      `resetDb()` étendu aux 2 tables.
- [x] `backend/models/link.js` : `createCode` (code 6 car. non ambigu, TTL, 1 code/user),
      `claimCode` ({ok,reason}: unknown/expired, 1-to-1, consomme le code), `findByDiscord`,
      `findBySteam`, `unlink`, `purgeExpired`.
- [x] `backend/routes/link.js` : `POST /link/claim` (auth webhook — 400/404/410/200) +
      `GET /link?discord=|steam=` ; monté dans `server.js` + liste d'endpoints.
- [x] `bot/commands/link.js` (`/link`) + `bot/commands/unlink.js` (`/unlink`) — *ephemeral*,
      auto-chargées par `deploy-commands.js` et `bot.js`.
- [x] Plugin v0.3.0 : `[Command("link")]` + `!link <code>` (OnUserChat) → `DoLink` POST
      `/link/claim { code, steam_id=IPlayer.Id, steam_name }`.
- [x] Docs : spec + plan, README (commandes + endpoints + roadmap), plugin/README.
- [x] `npm test` → **33/33** (23 + 10). Smoke test live OK : createCode → claim 200 →
      GET link (discord/steam) → re-claim 404 (consommé).

## Vérif restante (serveur Rust — non bloquant)
- Le `!link`/`/link` plugin (C# non testable) à confirmer en live (covalence `[Command]` +
  `IPlayer.Id` = SteamID64).

## Note merge
- Versions plugin : 4.1 = v0.3.0 ; la branche draft 3.2 est aussi v0.3.0 → au rebase de 3.2
  sur le futur `main`, repasser 3.2 en v0.4.0 et combiner (OnCrateHack + !link, sans conflit logique).

## Reporté (4.2+)
- Death notifications (s'appuie sur `links`), multi-serveur, PostgreSQL, déploiement.
