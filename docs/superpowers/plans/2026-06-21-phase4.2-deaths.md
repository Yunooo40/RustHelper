# Phase 4.2 — Death kill feed — Plan

Date : 2026-06-21 · Branche : `feat/phase4.2-deaths` (depuis `main`) · Spec :
[specs/2026-06-21-phase4.2-deaths-design.md](../specs/2026-06-21-phase4.2-deaths-design.md)

TDD côté route/API ; bot + plugin via fixture-contrat.

## Checklist
- [x] `backend/db.js` : table `deaths` (+ index). `resetDb()` étendu.
- [x] Tests rouges `tests/backend/death.test.js` + fixture `death-payloads.js`.
- [x] `shared/bus.js` : `DEATH_EVENT`.
- [x] `backend/models/death.js` : `insert`, `recent`.
- [x] `backend/routes/webhook.js` : `POST /webhook/death` (auth) — 400 si server/victim_name
      manquant ; résout `victim/killer_discord_id` via `Link.findBySteam` ; insert ; émet
      `DEATH_EVENT`.
- [x] `bot/lib/embeds.js` : `deathEmbed` (mention non-pingante si lié) ;
      `bot/bot.js` : `bus.on(DEATH_EVENT)` → poste dans le canal (factorisé `sendToChannel`).
- [x] Plugin v0.4.0 : config `Relay player deaths` (défaut false), `OnPlayerDeath`
      (NPC exclus) → `SendDeath` (killer/cause/distance best-effort, try/catch) POST `/webhook/death`.
- [x] Docs : spec + plan, README (REST + roadmap), plugin/README (config).
- [x] `npm test` → **39/39** (33 + 6). Smoke test live OK : victime liée → discordId résolu,
      non liée → null, 400 si victim_name manquant.

## Piège rencontré
Un backtick dans un commentaire SQL de `db.js` (`` `links` ``) fermait le template literal
`db.exec(\`...\`)` → `SyntaxError` sur tous les fichiers important l'app. Corrigé (texte simple).

## Vérif restante (serveur Rust — non bloquant)
- `OnPlayerDeath` + extraction killer/cause/distance (C# non testable) à confirmer en live
  (activer `Relay player deaths` + `Debug`).

## Reporté (4.3+)
- DM perso opt-in, stats K/D, multi-serveur, PostgreSQL, déploiement.
