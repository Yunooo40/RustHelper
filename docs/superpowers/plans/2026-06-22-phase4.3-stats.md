# Phase 4.3 — K/D stats & leaderboard — Plan

Date : 2026-06-22 · Sur `main` (push direct) · Spec :
[specs/2026-06-22-phase4.3-stats-design.md](../specs/2026-06-22-phase4.3-stats-design.md)

TDD côté modèle (logique K/D pure, 100% testable). Commandes/embeds = couches fines
Discord, validées par chargement (`createBot` + `data.toJSON()`).

## Checklist
- [x] `backend/models/stats.js` : `kd`, `forPlayer`, `leaderboard` (+ `bestStreakFor` interne).
- [x] `tests/backend/stats.test.js` (12) : comptage, suicide, NPC, zéro-division, streak,
      filtre serveur, noms récents, leaderboard tri/limit/minKills.
- [x] `bot/lib/embeds.js` : `statsEmbed`, `leaderboardEmbed`.
- [x] `bot/commands/stats.js` (`/stats [joueur]`) + `bot/commands/leaderboard.js`.
- [x] `npm test` → **51/51** (39 + 12). `createBot` charge 9 commandes ; `/stats` + `/leaderboard`
      sérialisent en JSON (deploy-commands OK).
- [x] Docs : spec + plan.

## Notes
- **Aucun changement plugin / DB / route** : la feature ne fait que lire `deaths`. Pas de
  migration, `resetDb()` inchangé.
- Bug attrapé en test (pas en prod) : un « bourreau de référence » qui tue plusieurs victimes
  accumule lui-même un K/D et pollue le classement → corrigé en infligeant les morts de
  référence via l'environnement (`killer = null`). Le code était correct.

## Vérif restante (Discord live — non bloquant)
- Lancer `npm run deploy-commands` puis tester `/stats`, `/stats @x`, `/leaderboard` en jeu
  une fois des morts enregistrées (dépend de `Relay player deaths` activé côté plugin, Phase 4.2).

## Reporté (4.4+)
- DM perso opt-in, stats par arme/wipe, multi-serveur réel, PostgreSQL, déploiement.
